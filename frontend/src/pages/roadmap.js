/**
 * Learning Roadmap page — implemented in Step 12.
 */
export async function renderRoadmap(container, user) {
  container.innerHTML = `
    <div class="stub-page">
      <h2>Learning Roadmap</h2>
      <p>Structured learning paths are coming soon. (Step 12)</p>
    </div>
  `;
}

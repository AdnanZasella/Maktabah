/**
 * Login page — implemented in Step 13.
 */
export async function renderLogin(container, user) {
  container.innerHTML = `
    <div class="stub-page">
      <h2>Login</h2>
      <p>User authentication is coming soon. (Step 13)</p>
    </div>
  `;
}

/**
 * Register page — implemented in Step 13.
 */
export async function renderRegister(container, user) {
  container.innerHTML = `
    <div class="stub-page">
      <h2>Create Account</h2>
      <p>User registration is coming soon. (Step 13)</p>
    </div>
  `;
}
